query FetchProductTypes {
  productTypes {
    results {
      key
      name
      attributeDefinitions {
        results {
          type {
            name
          }
          attributeConstraint
          name
        }
      }
    }
  }
}